{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}

<main class="container">
<div class="d-flex justify-content-between align-items-center">
    <h1>Categorie</h1>
    <a class="nav-link" href="{{ path('app_cate_index') }}">Retoure a la list de categorie</a>
</div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorie.name }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('cate/_delete_form.html.twig') }}
</main>
{% endblock %}

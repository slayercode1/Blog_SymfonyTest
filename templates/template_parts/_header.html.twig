<header class="navbar navbar-expand-lg navbar-light bg-light navbar-fixed-top mb-4 justify-content-around">
    <a class="navbar-brand mb-0 h1" href="{{path('app_accueil')}}">Blog.Org</a>
    <nav class="collapse navbar-collapse">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
            <a class="nav-link" href="{{path('app_accueil')}}">Accueil</a></li>
            <li class="nav-item">
            <a class="nav-link" href="{{path('app_categorie')}}">Categorie</a></li>
        </ul>
    </nav>
    <div class="form-inline">
        <ul class="navbar-nav">
            {% if not app.user %}
                <li class="nav-item"><a class="nav-link" href="{{path('app_register')}}">S'inscrire</a></li>
                <li class="nav-item btn btn-sm btn-outline-secondary"><a class="nav-link" href="{{path('app_login')}}">Se connecter</a></li>
            {% endif %}
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-items"><a class="nav-link" href="{{ path('app_article_index') }}">Dashboard</a></li>
                {% endif %}
                <li class="nav-item "><a class="nav-link" href="{{path('app_profil')}}">Mon Profil</a></li>
                <li class="nav-items"><a class="nav-link" href="{{ path('app_logout') }}">Logout</a></li>
            
        {% endif %}
        </ul>
    </div>
</header>